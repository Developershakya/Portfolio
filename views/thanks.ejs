<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You!</title>
    <style>
        /* Reset default margin and padding */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Centering the content */
        body, html {
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #70e1f5, #ffd194); /* Gradient background */
        }

        /* Container for the thank you message */
        .thank-you-container {
            max-width: 600px;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.9); /* White background with transparency */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Soft box shadow */
            border-radius: 8px;
            text-align: center;
        }

        /* Styling the content inside the container */
        .thank-you-content {
            padding: 40px 20px;
        }

        .thank-you-content h1 {
            font-size: 3em;
            color: #333333; /* Dark gray text color */
            margin-bottom: 20px;
        }

        .thank-you-content p {
            font-size: 1.2em;
            color: #666666; /* Medium gray text color */
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="thank-you-container">
        <div class="thank-you-content">
            <h1>Thank You!</h1>
            <p>Your message has been received.</p>
            <p>We will get back to you shortly.</p>
        </div>
    </div>
</body>
</html>
